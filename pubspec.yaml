name: geobank_novo
description: GPS de Prospecção Bancária

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.0.0
  firebase_auth: ^5.0.0
  cloud_firestore: ^5.0.0
  http: ^1.2.0
  google_maps_flutter: ^2.6.0
  geolocator: ^12.0.0
  url_launcher: ^6.2.5
  geocoding: ^3.0.0
  fl_chart: ^0.66.0 # 📊 Biblioteca de Gráficos

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
